nasm -f bin ./boot.asm -o ./boot.bin
qemu-system-x86_64 -hda ./boot.bin

Git:
git add .
git commit -m "message"
git push origin main


target remote | qemu-system-i386 -S -gdb stdio -hda ./os.bin
add-symbol-file ../build/kernelfull.o 0x100000
layout asm


new system siuu

#Das bedeutet, das under Kernel nach genau einem mb (1024) geladen wird. Das wäre adresse 100000(HEX). Also das ist dann das ORG.


Commenting done (understood it):
    boot.asm
    disk.c
    dish.h
    io.asm
    io.h 
    idt.asm
    idt.c
    idt.h
    kernel.asm
    heap.h
    heap.c
    kheap.c
    kheap.h